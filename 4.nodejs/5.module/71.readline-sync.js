import readlineSync from "readline-sync";

const answer1 = readlineSync.question("첫번째 숫자 입력: ");
console.log(`첫번째 숫자는 ${answer1}`);

const answer2 = readlineSync.question("두번째 숫자 입력: ");
console.log(`두번째 숫자는 ${answer2}`);
